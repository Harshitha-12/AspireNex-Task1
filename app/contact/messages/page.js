import MessageContent from "@/components/UI/Contact/message-content";

export const metadata = { title: "M Harshitha - Check Messages 📩", robots: "noindex" };

const Messages = () => {
  return <MessageContent />;
};

export default Messages;
